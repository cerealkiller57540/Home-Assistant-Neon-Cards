# Audit Configuration Home Assistant — 2026-05-22

Audit exhaustif de `/config/` (sans `home-assistant_v2.db`).

---

## ✅ Déjà traité

- **Clé Mistral consolidée** sur `/config/pyscript/modules/ha_secrets.py` (source unique). Doublons supprimés : `pyscript/secrets.py`, `pyscript/modules/secrets.py`, `heat_agent_summary.py` import depuis ha_secrets.
- **Pyscript dead code supprimé** : `modules/log_writer.py`, `modules/mistral_http.py`, `heat_pump_control.py.off`, `heat_pump_contro2l.py.off`, `heat_agent - Copie.py.off`.
- **mova-mower-card.js** : version `v12.765` ajoutée en tête.
- **Commentaire `_tokens` pop** ajouté dans `heat_agent.py:656`.
- **Ligne `# vibration_active`** supprimée de `configuration.yaml`.
- **`will-change:transform`** retiré de `heat-monitor-card.js:316` (SVG statique). Ligne 521 conservé (anime via keyframe).

---

## 🗑️ Reste à supprimer

- `/config/www/nixie-clock-card.backup.js` — backup non versionné
- `/config/themes/neo-tokyo-v3.yaml.old` — ancien thème
- `/config/zigbee2mqtt/configuration.yaml.bak`
- `/config/.storage/lovelace.dashboard_test` (206 KB) — dashboard test orphelin
- `/config/.storage/lovelace_resources.bak`

---

## 🔧 À refactorer

### 1. Tarifs EDF hardcodés
**Fichier** : `/config/pyscript/heat_agent.py:293–301`

```python
transitions = [20, 380, 480, 840, 860, 980, 1020, 1260, 1430, 1460]
```

Cassera si EDF migre vers Tempo. → Helper `input_text` versionné ou config externe.

---

## ✨ Quick wins

### Fichiers vides
- `/config/scripts.yaml`, `/config/scenes.yaml` — vides, OK tant que pas utilisés.

### Helper mal nommé
`/config/configuration.yaml:176–177` : `led_effect_active` → renommer en `led_cascading_effect_active` pour cohérence avec les autres helpers PAC.

---

## 🎨 Custom cards

### Bonne hygiène ✅
- `IS_IPAD`/`IS_LOW_POWER` préfixés (HMC_, NHV2_, NSW_…)
- `ha-card` : `contain` + `box-sizing` + variables thème
- SVG fluides avec `preserveAspectRatio`
- ResizeObserver généralisé

---

## 🚀 Idées d'amélioration

### Logging pyscript
`/config/logs/heat_agent_log.jsonl` et `heat_agent.log` n'ont pas de rotation. Croissance illimitée. → cron de purge mensuel ou logrotate.

### Splitter heat_agent.py
1500+ lignes monolithiques. Sépare contexte / décision LLM / actions HA en 3 modules pour testabilité.

---

## 📊 Performance

- DB : **6.4 GB** (sain pour 180j de recorder)
- Registry : 2.3 MB / ~2000 entités (normal)
- Recorder exclusions : OK
- `purge_keep_days: 180` OK

---

## 🚫 Faux positifs de l'audit initial (à ignorer)

1. ~~`heat_pump_bias_status` / `solar_forecast_summary` → helpers~~ : ce sont des sensors dérivés d'autres états, doivent rester des templates (un helper est user-driven).
2. ~~`sensor.ecodan_planned_bias` duplique state/attribute~~ : vérifié, une seule occurrence de chaque clé (`phase`, `temp_ext`, `temp_salon`).
3. ~~`heat_agent.py` historique avec clés dupliquées~~ : `decision.pop("_tokens")` retire ET retourne, comportement correct.
4. ~~Désactiver `_call_mistral_libre()`~~ : utile pour comparaison guidée vs libre, garder en observation jusqu'octobre.
5. ~~PAC race condition `mode: single`~~ : vérifié, l'automation `smart_heat_pump_bias_control` est en `mode: restart` (l963), comportement correct.
6. ~~Supprimer `input_number.car_battery_max`~~ : encore utilisé en trigger/condition par 3 automations EV. Le slider sert toujours côté YAML, même si `car_charging.py` utilise une autre source.

---

## 📋 État global

| Catégorie | Count | Status |
|-----------|-------|--------|
| Automations | 34 | ⚠️ 1× race condition (PAC) |
| Pyscripts | 4 actifs | ✅ Clean après ce passage |
| Custom Cards | 40 | ✅ Conformes CLAUDE.md |
| Template Sensors | 40+ | ✅ |
| Custom Components | 17 | ✅ |
| Packages | 3 | ✅ |

---

## 🔑 Restant — priorités

1. **🟢 Bloat** : virer les .bak/.old/dashboard_test
2. **🔵 Tarifs EDF** : externaliser les transitions hardcodées
3. **⚪ Logs rotation** : cron purge mensuel sur heat_agent.jsonl
4. **⚪ Helper renommage** : `led_effect_active` → `led_cascading_effect_active`

---

**Audit date** : 2026-05-22  
**Maison** : RT2012, PAC Ecodan + solaire 6kW
